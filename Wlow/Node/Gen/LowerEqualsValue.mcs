~include(Api/operator.mcs.api)
~include(Api/comp.mcs.api)

using LLVMSharp.Interop;
using Wlow.Types;
using Wlow;

namespace Wlow.Node;

public readonly partial record struct LowerEqualsValue {
  ~define('isuf():Int)
  ~define('isufo():Int)
  ~define('iop:BuildICmp)
  ~define('udo:ULE)
  ~define('ido:SLE)
  'operator_infix(comp, lower or equals)
}
