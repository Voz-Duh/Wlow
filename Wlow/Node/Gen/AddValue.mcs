~include(Api/operator.mcs.api)
~include(Api/arith.mcs.api)

using LLVMSharp.Interop;
using Wlow.Types;

namespace Wlow.Node;

public readonly partial record struct AddValue {
  ~define('iop:BuildAdd)
  'operator_infix(arith, addition)
}
