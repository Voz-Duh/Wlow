~include(implicits.mcs.api)

using LLVMSharp.Interop;

namespace Wlow.Types;

public partial class GenericLinkMeta {
  ~define('body:
    'on(type,
      this.CurrentType = this.CurrentType.ImplicitCast(sc, info, to);
      return this;
    )
    'on(cast,
      return this.CurrentType.ImplicitCast(sc, info, val, to, generic_frendly);
    )
  )
  'implicits(body, generic link)
}
