~include(implicits.mcs.api)

using LLVMSharp.Interop;

namespace Wlow.Types;

public partial class VoidMeta {
  ~define('body:
    'if(VoidMeta,
      'on(type, return to;)
      'on(cast, return val;)
    )
  )
  'implicits(body, void)
}
