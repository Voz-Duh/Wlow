using LLVMSharp.Interop;

namespace Wlow.Types;

public partial class VoidMeta {
  ~include(implicits.mcs.api)
  ~define('body:
    'if(VoidMeta,
      'on(type, return to;)
      'on(cast, return val;)
    )
  )
  'implicits(body, void)

  ~include(explicits.mcs.api)
  ~define('body:
    'if(VoidMeta,
      'on(type, return to;)
      'on(cast, return val;)
    )
  )
  'explicits(body, void)
}
